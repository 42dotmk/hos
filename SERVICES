# services enabled in the rootfs: /var/service/NAME -> /etc/sv/NAME, one
# per line. Two gettys (hos's own service dirs, overlay/etc/sv); udevd,
# dbus and NetworkManager come with their packages.
agetty-tty1
agetty-tty2
udevd
dbus
NetworkManager
