# live iso: no early KMS. dracut's drm module would otherwise pull ~140M
# of GPU firmware (nvidia, amdgpu, i915) into the initramfs; the same
# firmware and drivers load from the squashfs after switch_root.
omit_dracutmodules+=" drm "
