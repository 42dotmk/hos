# hos ships no polkit: with it NetworkManager would refuse every request
# from a user ("Insufficient privileges" for nmcli as hos). Off, the
# D-Bus policy alone decides, and it admits any local user.
[main]
auth-polkit=false
