# installed on top of base-system (mklive default); one per line, # comments
# no vi, no vim, no editors but hed

# X
xorg-minimal
xinit
mesa-dri
dejavu-fonts-ttf
dbus
NetworkManager

# login shell (live.shell= on the cmdline, /etc/default/useradd for
# installed systems); hwm's autostart also runs through /bin/zsh -c —
# without it hbg/htray/hnd never start. The rest are what that list invokes.
zsh
setxkbmap
xset
picom
xss-lock
slock

# hackable tool runtime deps
SDL2
SDL2_ttf
libXft
fzf
libwebkit2gtk41
alsa-lib
vulkan-loader

# toolchain — /usr/src/hackable is meant to be rebuilt in place
gcc
make
pkg-config
git
cmake

# branding: /etc/fastfetch/config.jsonc draws /usr/share/hos/logo.txt
fastfetch

# fetching things
curl
wget

# hed's optional helpers
ripgrep
tmux
ctags

# file manager in the terminal (uses fzf/ripgrep above when present)
yazi

# headers for rebuilding every tool on the running system
libX11-devel
libXrandr-devel
libXrender-devel
libXcomposite-devel
libXdamage-devel
libXfixes-devel
libXtst-devel
libXft-devel
libinput-devel
eudev-libudev-devel
dbus-devel
SDL2-devel
SDL2_ttf-devel
openssl-devel
libcurl-devel
sqlite-devel
alsa-lib-devel
vulkan-loader-devel
libwebkit2gtk41-devel
# hstt's whisper.cpp vulkan backend compiles shaders at build time
shaderc
SPIRV-Headers
